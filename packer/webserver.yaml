- name: configure webserver
  hosts: webserver
  tasks:
    - name: Add epel-release repo
      yum:
        name: epel-release
        state: present
    - name: install nginx
      yum:
        name: nginx
